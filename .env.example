# DuckLake Configuration
#
# Two modes are supported:
# 
# LOCAL MODE (for development, no infrastructure needed):
#   Set DUCKLAKE_CATALOG_DSN to a file path
#   Set DUCKLAKE_DATA_PATH to a local directory
#   No S3 configuration needed
#
# POSTGRES MODE (for production):
#   Set DUCKLAKE_CATALOG_DSN to a Postgres DSN (contains 'host=')
#   Set DUCKLAKE_DATA_PATH to an S3 path (starts with 's3://')
#   Configure all DUCKLAKE_S3_* variables

# --- Local dev mode (Postgres catalog + local files) ---
DUCKLAKE_CATALOG_DSN="dbname=ducklake_catalog host=postgresql user=dagster_user password=dagster_password port=5432"
DUCKLAKE_DATA_PATH=./data/
DUCKLAKE_SCHEMA=local

# --- Postgres mode example (for production) ---
# DUCKLAKE_CATALOG_DSN="dbname=ducklake_catalog host=localhost user=dagster_user password=dagster_password port=5432"
# DUCKLAKE_DATA_PATH=s3://ducklake/data/
# DUCKLAKE_SCHEMA=sywi
# DUCKLAKE_S3_REGION=us-east-1
# DUCKLAKE_S3_ENDPOINT=localhost:9000
# DUCKLAKE_S3_ACCESS_KEY_ID=minioadmin
# DUCKLAKE_S3_SECRET_ACCESS_KEY=minioadmin
# DUCKLAKE_S3_URL_STYLE=path
# DUCKLAKE_S3_USE_SSL=false

# --- Remote DuckLake (for ./dev.sh pull command) ---
# Used to pull tables from production to local development
# These are always Postgres + S3 since the remote is production
#
# DUCKLAKE_REMOTE_CATALOG_DSN="dbname=ducklake_catalog host=production-server.com user=... password=... port=5432"
# DUCKLAKE_REMOTE_DATA_PATH=s3://ducklake/data/
# DUCKLAKE_REMOTE_S3_REGION=us-east-1
# DUCKLAKE_REMOTE_S3_ENDPOINT=production-minio.com:9000
# DUCKLAKE_REMOTE_S3_ACCESS_KEY_ID=...
# DUCKLAKE_REMOTE_S3_SECRET_ACCESS_KEY=...
# DUCKLAKE_REMOTE_S3_URL_STYLE=path
# DUCKLAKE_REMOTE_S3_USE_SSL=false
